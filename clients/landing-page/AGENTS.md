Do not add tests in this folder.

Legal documents live in `src/content/legal/*.md` and nowhere else. Keep them in
markdown, one sentence per line, so every version shows a readable diff. Never move
their text into TypeScript, JSON, or a component.
