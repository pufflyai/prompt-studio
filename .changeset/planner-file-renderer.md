---
"pstdio-planner": minor
---

Render ticket content with the native file renderer instead of a webview: the ticket body and each ticket file or image attachment open as their own resource, fixing slow ticket load.
