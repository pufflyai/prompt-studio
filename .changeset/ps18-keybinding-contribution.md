---
"@pstdio/sdk": minor
"pstdio": patch
"extension-lab": patch
---

Add extension keybinding contribution API so extensions can register app-level keyboard shortcuts for commands that fire even when focus is outside the extension's webview.
