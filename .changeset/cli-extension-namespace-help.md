---
"pstdio": patch
---

CLI: list extension command namespaces (e.g. `tickets`) in `pst --help`, show the namespace's commands for a bare or mistyped extension namespace, and align extension namespace help with the yargs layout.
