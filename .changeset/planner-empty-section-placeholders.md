---
"pstdio-planner": patch
---

Ticket sidebar: render empty Files and Workspaces sections as non-interactive empty states instead of disabled placeholder rows, and opt the Files/Workspaces/Sub-tickets categories into the sidebar's hide/show menu (canHide) so categories — never individual files or workspaces — can be customized.
