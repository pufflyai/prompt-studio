---
"@pstdio/ui": minor
---

Add a serializable `resource` ParamEditor type that renders resource references as tags, editable (dropdown) or view-only, where a chip can open an external link, open a host resource, or copy text to the clipboard.
