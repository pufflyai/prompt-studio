---
"pstdio": minor
---

Add `pstdio extensions check` diagnostics command that loads installed extensions from `~/.pstdio/extensions` via the `pstdio-extensions` runtime and reports normalized registry entries plus any diagnostics.
