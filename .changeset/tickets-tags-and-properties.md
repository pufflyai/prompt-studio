---
"pstdio": minor
"pstdio-core-tickets": minor
---

Register extension-contributed settings panels in the dashboard so the ticket and workspace status editors are reachable and editable. Add ticket tags (tag definitions + options with seeded defaults, board enum-multi attributes, drag/inline support, and a "Ticket tags" settings panel) and a ticket detail properties panel in the editor (editable status + tags; id / updated / parent / depends-on / archived shown).
