---
"pstdio": patch
---

Extension menu actions that declare input (e.g. Run attempt, Refine ticket) now open the shared params dialog to collect agent/model, repository, template, and context before running, instead of executing immediately with no prompt.
