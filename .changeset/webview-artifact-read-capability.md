---
"pstdio": minor
"@pstdio/sdk": minor
"@pstdio/workbench": minor
"pstdio-skills": patch
---

Add a mount-scoped `artifacts.read` webview capability with typed client reads (list, readText, imageUrl), host-enforced mount confinement, size limits, a raster-image allowlist, and short-lived signed image URLs
