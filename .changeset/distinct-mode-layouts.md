---
"pstdio": patch
---

Layout model now removes orphan placements when a widget contribution is disposed. Refactor the Random shell example (renamed from Notes) so each mode (Notes, Music, Mail) registers its own widgets and renders a distinct layout — a notebook, a media player with playback controls, and a 3-pane email client.
