---
"pstdio-planner": patch
---

Persist planner archive-all ticket updates before linked workspace cleanup, and report cleanup failures via a persistent notification so a failed cascade no longer rejects the archive command.
