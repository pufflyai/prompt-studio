---
"pstdio": minor
---

Rename bundled template files to `name.[template|prompt].md`, split the code-review prompt into a `review-code` prompt plus a new `code-review` document template, and remove the legacy `review-me` document template.
