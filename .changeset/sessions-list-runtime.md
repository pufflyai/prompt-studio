---
"pstdio": patch
---

Implement the extension `sessions.list()` command-environment API (returns the project's sessions with their anchors), and open sessions hinted with `sessionSurface: "floating"` in the dashboard's floating panel instead of switching to sessions mode
