---
"pstdio": patch
---

Re-apply a tree renderer's default expanded sections on registration so default-expanded sections (e.g. a ticket's workspaces) start expanded even when an older persisted state predates them.
