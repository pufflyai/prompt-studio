---
"pstdio": patch
---

Refresh a repo-local extension in its own directory, serve only the commands its source defines now, keep one enabled provider per extension id, and show each extension's source folder.
