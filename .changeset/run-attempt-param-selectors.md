---
"pstdio": patch
---

Render harness and repository selectors instead of a raw JSON field in the command params dialog, remember the last selected harness, and drop the raw command id shown as the dialog subtitle.
