---
"pstdio": minor
"pstdio-planner": minor
---

Let extensions contribute their own settings sections through a `settingsSections` contribution that panels opt into with `section`, and group the planner's Ticket status and Ticket tags panels under a Planner section; adopt status ring and level bar glyphs as the default status and tag icons
