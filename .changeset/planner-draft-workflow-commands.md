---
"pstdio-planner": minor
---

Add the ticket draft workflow (`write`/`save`/`pull`/`files`), `implement`, `update-when-attempt-status`, and workspace/worktree listing as planner extension commands backed by extension storage and the host file/session/workspace primitives. Registered with `tickets …` CLI aliases (dormant until core dispatch is made generic).
