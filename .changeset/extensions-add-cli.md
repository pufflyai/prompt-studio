---
"pstdio": minor
---

Add `pstdio extensions add` installer command that copies extension source into `~/.pstdio/extensions/<install-name>` from a local folder or the `pufflyai/prompt-studio` GitHub repo, validates the installed source through the `pstdio-extensions` runtime, and supports `--name` and `--force`.
