---
"@pstdio/workbench": patch
"pstdio": patch
---

Add the workbench composition resolver: one reconciliation pass restores required placements on every context activation, panel menus follow their owner panel, and layouts from older schema versions are discarded
