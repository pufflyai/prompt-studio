---
"pstdio-planner": patch
---

Archiving a ticket now archives its workspaces; setting a workspace status no longer re-runs its automation when unchanged; status/tag reorder and a real workspace-status default persist; and ticket links are derived from generic resource anchors.
