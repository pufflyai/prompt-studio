---
"pstdio": minor
---

Render extension-contributed data renderers in the dashboard. A new extensions module loads project extension metadata and surfaces each declared data renderer as a project-sidebar view whose board/list is backed by the extension's query and mutation commands over REST.
