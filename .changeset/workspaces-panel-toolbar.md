---
"pstdio": patch
"@pstdio/ui": patch
---

Add a bottom border to the data renderer toolbar, and show filter/display controls on the dashboard workspaces panel (defaulting to a list view, ungrouped, ordered by created date, filterable by type).
