---
"pstdio": patch
"@pstdio/ui": patch
"@pstdio/workbench": patch
"pstdio-planner": patch
---

Keep the ticket context when opening workspace files, block sending a message without a model, merge harness run defaults into the default model setting, and return to the ticket body when the open ticket file is deleted.
