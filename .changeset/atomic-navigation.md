---
"@pstdio/workbench": patch
"pstdio": patch
---

Add the atomic workbench navigator: mode and resource commit together with one layout-scope rotation, incompatible modes restore their last or default compatible resource, and history replay uses the same transaction
