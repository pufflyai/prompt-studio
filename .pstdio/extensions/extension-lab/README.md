# Extension Lab

Local v2 extension used to validate PS-77 extension runtime work as new platform features land.

Run from the project root:

```bash
pstdio extensions check
```

Use the local branch command while developing the CLI:

```bash
bun run --cwd packages/pstdio pstdio -- extensions check
```
