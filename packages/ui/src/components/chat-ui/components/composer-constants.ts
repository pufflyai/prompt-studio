// Shared height for every composer control row (editor, attach, model, send).
// Maps to the design's `composer-ctl-h` (28px = Chakra sizes token `7`), so the
// editor row and the toolbar controls line up at one height.
export const COMPOSER_CONTROL_HEIGHT = "7";
