import { defineLayerStyles } from "@chakra-ui/react";

export const layerStyles = defineLayerStyles({
  modal: {
    value: {
      paddingInline: "xs",
      paddingBlock: "sm",
      borderRadius: "sm",
      bg: "bg",
      border: "1px solid",
      borderColor: "border",
    },
  },
});
