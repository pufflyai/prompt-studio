{
  "name": "pstdio",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "pstdio": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "pstdio": "bun ./src/index.ts",
    "build": "bun build ./src/index.ts --outfile dist/index.js --target node --banner \"#!/usr/bin/env node\" --external ink --external react --external ink-text-input",
    "analyze-bundle": "bun build ./src/index.ts --outfile dist/index.js --target node --banner \"#!/usr/bin/env node\" --external ink --external react --external ink-text-input --metafile=./dist/bundle-meta.json --metafile-md=./dist/bundle-meta.md",
    "build:bundle": "bun run build && mkdir -p dist/api && cp -r ../pstdio-dashboard/dist ./dist/dashboard && cp ../pstdio-api/dist/index.js ./dist/api/server.js",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build",
    "lint": "bun run typecheck && bunx @biomejs/biome lint .",
    "format": "bunx @biomejs/biome format --write .",
    "test": "bun test"
  },
  "dependencies": {
    "@tanstack/react-db": "^0.1.74",
    "ink": "^6.8.0",
    "ink-markdown": "^1.0.4",
    "ink-text-input": "^6.0.0",
    "pstdio-agents": "workspace:*",
    "react": "^19.2.4",
    "yargs": "^18.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.3.0",
    "@types/react": "^19.2.14",
    "@types/yargs": "^17.0.35",
    "typescript": "~5.9.3"
  }
}
