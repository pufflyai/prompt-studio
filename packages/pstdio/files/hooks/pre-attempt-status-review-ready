#!/bin/sh
set -eu

# ──────────────────────────────────────────────────────────
# Pre-hook: validate before allowing transition to review-ready
# Rejects the transition if the command below exits non-zero.
#
# Replace the command below with your validation step.
# ──────────────────────────────────────────────────────────

REPO_PATH=${PSTDIO_WORKTREE_PATH:-$(pwd)}

cd "$REPO_PATH"

# >>> Replace with your validation command <<<
echo "No validation configured"
