import { Canvas, Meta, Source } from "@storybook/addon-docs/blocks";
import * as OnboardingStories from "./onboarding.stories";
import source from "./extensions/tree-page-extension.ts?raw";

<Meta of={OnboardingStories} name="3. First extension page" />

# 3. Build the first extension page

This is the smallest useful workbench extension in the guide. It declares a native tree view, places that view on a
page, and adds a navigation entry that opens the page.

<Canvas of={OnboardingStories.FirstExtensionPage} sourceState="shown" />

## Complete example

The story runs this extension definition. It uses only the public Extension API from `@pstdio/sdk/extensions`.

<Source code={source} language="tsx" />

## What each field means

`defineView(...)` creates reusable content:

- `id` is the View's stable local identity. The returned `guideTree.ref` is the typed reference other definitions use.
- `title` is its default user-facing label.
- `body.kind: "tree"` asks the host to render the shared native tree UI.
- `defaultExpandedSectionIds` opens the named section on first use.
- the inner `body` function loads the tree sections and nodes. Section and node ids remain stable across refreshes;
  labels and icons are visible to the user.
- `selected: true` marks the node the host highlights as the current selection.

`definePage(...)` creates the location that owns the View placement:

- `id` creates `guidePage.ref`.
- `title` is the page label.
- `path` is the page's stable URL segment.
- `mode: workbenchModes.project` makes the page available in Project mode.
- `slots` is the complete page layout. The `content` slot is the primary location, belongs in `main`, and renders
  `guideTree.ref`.

`defineNavigationItem(...)` creates the action visible inside the workbench:

- `owner` puts the item in Project mode's navigation tree.
- `slot: "content"` places it in the scrolling middle area, below pinned header content and above pinned footer content.
- `group: "Learn"` places it under a visible Learn section.
- `label` and `icon` describe the row.
- `action` is a navigation target. `{ kind: "page", page: guidePage.ref }` opens the Guide page. Page navigation
  replaces the previous page placements instead of appending to them.

Finally, `defineExtension(...)` exports all four definitions as one installable unit. The host loads, validates, and
removes them together. Registering the View alone would show nothing; the page places it, and the navigation item gives
the user a way to reach it.
