# pstdio Reports

Workspace-scoped reports for agent handoffs.

Use `pst reports write --kind review`, edit `.pstdio/reports/<name>/report.md`, add supporting files under `.pstdio/reports/<name>/files/`, then run `pst reports save --name <name>`.
